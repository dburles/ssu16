import React from 'react';
import SampleParameters from './SampleParameters';

const SampleParametersContainer = ({ state, dispatch }) => {
  return <SampleParameters volume={100} />;
};

export default SampleParametersContainer;
